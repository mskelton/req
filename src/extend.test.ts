import { describe, it } from "vitest"

describe("extend", () => {
  it.todo("should copy baseURL from parent instance")
  it.todo("should merge headers from parent instance")
  it.todo("should merge hooks from parent instance")
})
